# QCMS v4.13.5 — Maroon Sections, White Fields & KPI Icon Fix

Build: `4135-MAROON-SECTIONS-WHITE-FIELDS-KPI-ICON-FIX`

- Every collapsible section title is enforced as a bold maroon heading across QCMS.
- All value/input pockets now use the same white surface as list/select fields; cream/yellow field fills are removed by the final CSS contract.
- KPI/status cards reserve a dedicated icon column so status icons cannot merge with labels or numeric values.
- Existing v4.13.4 RMTC reuse, duplicate-safe import and business logic are preserved unchanged.
- No Supabase schema migration is required.

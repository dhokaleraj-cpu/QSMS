# Install QCMS Mobile 1.0.2 on Samsung

Use `BUILD_AND_INSTALL_SAMSUNG.command` with USB debugging enabled. The helper builds the debug APK, verifies APK Signature Scheme v2, installs package `com.fourstar.qcms.test`, and launches it.
